# OMO Hybrid Landing Page

Static landing page exported as `index.html`.

## Files

- `index.html` - landing page entry point.
- `assets/` - supporting images from the attached Google Drive export.

## Deploy

This repo can be published with GitHub Pages by setting the Pages source to the repository root.
